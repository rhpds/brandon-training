---
containers: []
virtualmachines:
  - name: "host1"
    image: "rhel-10.1"
    memory: "8G"
    cores: 1
    image_size: "40G"
    tags:
      - key: "AnsibleGroup"
        value: "bastions"
    networks:
      - default
      - secondary
    services:
      - name: cockpit
        ports:
          - port: 9100
            protocol: TCP
            targetPort: 9100
            name: cockpit
    routes:
      - name: cockpit
        host: cockpit
        service: cockpit
        targetPort: 9100
        tls: true
        tls_termination: Edge
